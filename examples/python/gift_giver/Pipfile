[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
expects = "~=0.9"
mamba = "~=0.10"
pycodestyle = "~=2.5"

[packages]
flask = "~=1.1"
python-dotenv = "~=0.12"
stories = "~=0.12"
flask-api = "~=2.0"
requests = "~=2.23"
orator = "~=0.9"

[requires]
python_version = "3.7.0"

[scripts]
server = "flask run"
lint = "pycodestyle ./ --max-line-length=120"
test = "mamba"
console = "python"
migrate = "orator migrate -f"
